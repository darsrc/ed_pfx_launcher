; ============================================================
; ed_pfx_launcher.ini
; Shareable configuration for ed_pfx_launcher.sh
; ============================================================

[steam]
; Elite Dangerous (base game) AppID
appid=359320

; Optional overrides (auto-detection usually works on native installs)
; steam_root={home}/.local/share/Steam
; compatdata_dir={steam_root}/steamapps/compatdata/{appid}          ; DEPRECATED -> use prefix_dir
; prefix_dir={steam_root}/steamapps/compatdata                      ; search root or exact compatdata/<appid> path
; prefix_select=first                                               ; first|newest
; --interactive can auto-detect and write your chosen prefix_dir/proton.dir
; runtime_client={steam_root}/steamapps/common/SteamLinuxRuntime_sniper/pressure-vessel/bin/steam-runtime-launch-client

[prefix]
; Optional explicit Wine prefix directory.
; If unset, the script auto-detects from STEAM_COMPAT_DATA_PATH,
; steam.compatdata_dir, and common Steam compatdata locations.
; dir={steam_root}/steamapps/compatdata/{appid}/pfx

[proton]
; Optional explicit Proton directory (contains the proton script).
; If unset, the script auto-detects from Steam common and compatibilitytools.d.
; dir={home}/.steam/steam/compatibilitytools.d/GE-Proton10-32

; ---- Deprecated keys (compatibility read-only for one release cycle) ----
; proton={home}/.steam/steam/compatibilitytools.d/GE-Proton10-32/proton
; dir={home}/.steam/steam/compatibilitytools.d                      ; search root or exact Proton* directory
; select=first                                                      ; first|newest



[instances]
; Optional component-scoped compatdata directories (or exact compatdata/<appid> paths).
; Missing keys automatically fall back to steam.prefix_dir behavior.
; game_prefix_dir={steam_root}/steamapps/compatdata
; edcopilot_prefix_dir={steam_root}/steamapps/compatdata
; edcopter_prefix_dir={steam_root}/steamapps/compatdata
; tool_prefix_dir={steam_root}/steamapps/compatdata

[shared_data]
; Share Elite Dangerous user-data directories between component prefixes.
; enabled=true to bridge selected directories from source_prefix into other prefixes.
; source_prefix=game          ; game|edcopilot|edcopter|tool
; strategy=symlink            ; symlink preferred, bind/copy reserved for future fallback
enabled=true
source_prefix=game
strategy=symlink

; Exact Windows paths mapped under drive_c (relative to each Wine prefix):
; map_appdata_frontier=users/steamuser/AppData/Local/Frontier Developments/Elite Dangerous
; map_appdata_edcopilot=users/steamuser/AppData/Local/EDCoPilot
; map_documents_frontier=users/steamuser/Documents/Frontier Developments/Elite Dangerous
map_appdata_frontier=users/steamuser/AppData/Local/Frontier Developments/Elite Dangerous
map_appdata_edcopilot=users/steamuser/AppData/Local/EDCoPilot
map_documents_frontier=users/steamuser/Documents/Frontier Developments/Elite Dangerous

[interactive]
; Temporary UI rollout toggle for --interactive.
; wizard (default): full-screen wizard UI when terminal supports it.
; legacy: prior menu/prompt flow (deprecated, retained for one release window).
; CLI --interactive-ui overrides this setting.
ui=wizard

[detection]
; Timeout for launcher detection (seconds)
launcher_timeout=120

; Timeout for game detection after launcher is seen (seconds)
game_timeout=120

[edcopilot]
; Master enable
enabled=true

; Path to EDCoPilot launcher executable
; exe={prefix}/drive_c/EDCoPilot/LaunchEDCoPilot.exe

; Preferred launch mode for deterministic behavior:
; runtime: require runtime launch path (recommended)
; auto: compatibility mode (legacy behavior)
; wine: force Proton run
mode=wine

; Delay before launching EDCoPilot (seconds)
startup_delay=30

; How long to wait for runtime/client evidence before mode resolution (seconds)
bus_wait=30

; How long to wait for EDCoPilotGUI2.exe after launch (seconds)
init_timeout=45

; During shutdown, after writing EDCoPilot.request.txt=Shutdown,
; wait this many seconds for EDCoPilot processes to exit naturally.
graceful_shutdown_timeout=15

; ---- Deprecated keys (compatibility read-only for one release cycle) ----
; delay=30                    ; DEPRECATED -> use startup_delay
; shutdown_timeout=15         ; DEPRECATED -> use graceful_shutdown_timeout

; Existing optional compatibility settings
force_kill_timeout=5
allow_proton_fallback=true
force_linux_flag=true

; Prefer relative path inside prefix if exe is unset
exe_rel=drive_c/EDCoPilot/LaunchEDCoPilot.exe

[shutdown]
; Monitor target controls lifecycle trigger point:
; launcher|game
monitor_target=game

; Keep helper tools running when game exits (recommended).
; true: close EDCoPilot/EDCoPTER and child processes during coordinator shutdown
; false: leave tools running and skip wineserver cleanup
close_tools_with_game=false

; If true, run wineserver cleanup sequence (-k then -w)
wineserver_cleanup=false

[audio]
; Mitigates crackling for some Proton/Wine audio setups.
pulse_latency_msec=90

; ---- Deprecated keys (compatibility read-only for one release cycle) ----
; [wine] wineserver_kill_on_shutdown=true ; DEPRECATED -> shutdown.wineserver_cleanup

[elite]
; MinEdLauncher-only policy:
; this script always launches MinEdLauncher.exe and will not run EDLaunch.exe.
; Optional: explicit path if MinEdLauncher.exe is somewhere unusual.
; Launcher selection policy:
; mined (default): use MinEdLauncher.exe (recommended for Linux setups)
; mined: force MinEdLauncher.exe
; auto: try EDLaunch.exe, fallback to MinEdLauncher.exe
launcher_preference=mined

; Optional: explicit launcher paths if your install is non-standard
; edlaunch_exe=/run/media/you/1TB/Games/EliteDangerous/EDLaunch.exe
; mined_exe=/run/media/you/1TB/Games/EliteDangerous/MinEdLauncher.exe

; Platform auth mode for MinEdLauncher.
; frontier (default): requires elite.profile or /frontier in mined_flags.
; steam: allows Steam auth path.
platform=frontier

; MinEd launch arguments (space-separated)
; Defaults are chosen to avoid interactive product selection prompts.
; mined_flags=/autorun /autoquit /edo

; Optional launcher profile injected as: /frontier <profile>
; WARNING: Leaving profile blank in frontier mode requires /frontier in mined_flags,
; otherwise MinEdLauncher will try Steam auth and crash.
profile=darvix

[edcopter]
; Optional: disabled by default for shareability
enabled=false
shutdown_timeout=5
; exe_rel=drive_c/Program Files/EDCoPTER/EDCoPTER.exe

[wine]
; DEPRECATED (compatibility-only): use [shutdown] wineserver_cleanup.
; wineserver_kill_on_shutdown=false
; wineserver_wait_on_shutdown=false
